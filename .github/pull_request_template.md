**What this change does**

**Why**

**Checklist**

- [ ] `gofmt -l .` gives no output
- [ ] `go vet ./...` passes
- [ ] `go test -race ./...` passes
- [ ] Tests cover the change
- [ ] The changelog records the change, if a user can see it
