version: "2"

# The lint job checks the formatting as well, so a badly formatted file
# cannot pass CI. "golangci-lint fmt" puts it right.
formatters:
  enable:
    - gofmt

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
  settings:
    revive:
      rules:
        # Many parameters exist only to satisfy an interface or a state
        # machine signature.
        - name: unused-parameter
          disabled: true
        - name: package-comments
          disabled: true
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
